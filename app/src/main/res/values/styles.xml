<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/numeros</item>
        <item name="colorPrimaryDark">@color/numeros</item>
        <item name="colorAccent">@color/numeros</item>
    </style>

</resources>
